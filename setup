# make vimrc

echo "set expandtab smarttab tabstop=2 softtabstop=0 shiftwidth=2
set whichwrap+=<,>,h,l,[,]" > ~/.vimrc

# setup vim as default editor for sudoedit

echo "export EDITOR="/usr/bin/vim"" >> ~/.bashrc
